all:
  children:
    weblaunch_servers:
      hosts:
        weblaunch-app-01:
          ansible_host: 10.0.1.10
          ansible_user: ubuntu
        weblaunch-app-02:
          ansible_host: 10.0.1.11
          ansible_user: ubuntu
      vars:
        ansible_python_interpreter: /usr/bin/python3
        environment: production

    monitoring_servers:
      hosts:
        weblaunch-monitor-01:
          ansible_host: 10.0.2.10
          ansible_user: ubuntu
      vars:
        ansible_python_interpreter: /usr/bin/python3
