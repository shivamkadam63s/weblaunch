---
- name: restart prometheus
  community.docker.docker_container:
    name: weblaunch-prometheus
    state: started
    restart: yes

- name: restart monitoring stack
  community.docker.docker_compose_v2:
    project_src: /opt/monitoring
    state: present
    recreate: always
