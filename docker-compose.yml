version: "3.9"

# ============================================================
#  WebLaunch - Automated Website Deployment Platform
#  Single docker-compose.yml for the full stack
# ============================================================

x-common-labels: &labels
  com.weblaunch.project: "weblaunch"

networks:
  weblaunch-net:
    driver: bridge
  monitoring-net:
    driver: bridge

volumes:
  redis-data:
  prometheus-data:
  grafana-data:
  k3s-data:
  k3s-config:

services:

  # ── Frontend (React + Vite → Nginx) ───────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: weblaunch-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-3002}:80"
    environment:
      - VITE_API_URL=http://backend:4000
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - weblaunch-net
    labels:
      <<: *labels
      com.weblaunch.service: "frontend"

  # ── Kubernetes (K3s - Lightweight K8s in Docker) ──────────
  k3s:
    image: rancher/k3s:v1.29.3-k3s1
    container_name: weblaunch-k3s
    entrypoint: /bin/sh
    command: >
      -c "
      mkdir -p /etc/rancher/k3s &&
      echo 'mirrors:
        \"registry:5000\":
          endpoint:
            - \"http://registry:5000\"' > /etc/rancher/k3s/registries.yaml &&
      exec /bin/k3s server --write-kubeconfig-mode 644 --tls-san=k3s --tls-san=weblaunch-k3s
      "
    privileged: true
    restart: unless-stopped
    environment:
      - K3S_KUBECONFIG_OUTPUT=/output/k3s.yaml
      - K3S_KUBECONFIG_MODE=666
    volumes:
      - k3s-data:/var/lib/rancher/k3s
      - k3s-config:/output
    ports:
      - "6443:6443"
      - "8081:80"
      - "8443:443"
    networks:
      - weblaunch-net
    labels:
      <<: *labels
      com.weblaunch.service: "k3s"

  # ── Backend (Node.js / Express) ────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: weblaunch-backend
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-4000}:4000"
    environment:
      - PORT=4000
      - NODE_ENV=${NODE_ENV:-production}
      - FRONTEND_URL=http://localhost:${FRONTEND_PORT:-3000}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      - DOCKER_SOCKET=/var/run/docker.sock
      - K8S_NAMESPACE=${K8S_NAMESPACE:-weblaunch}
      - API_KEY=${API_KEY:-}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - KUBECONFIG=/root/.kube/k3s.yaml
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./backend/logs:/app/logs
      - k3s-config:/root/.kube:ro
    depends_on:
      redis:
        condition: service_healthy
      k3s:
        condition: service_started
    healthcheck:
      test: [ "CMD", "wget", "-qO-", "http://localhost:4000/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - weblaunch-net
      - monitoring-net
    labels:
      <<: *labels
      com.weblaunch.service: "backend"

  # ── Local Registry (for K8s to pull images) ────────────────
  registry:
    image: registry:2
    container_name: weblaunch-registry
    restart: unless-stopped
    ports:
      - "5000:5000"
    networks:
      - weblaunch-net
    labels:
      <<: *labels
      com.weblaunch.service: "registry"

  # ── Redis (Job Queue + Store) ──────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: weblaunch-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --appendfsync everysec
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - weblaunch-net
    labels:
      <<: *labels
      com.weblaunch.service: "redis"

  # ── Prometheus ─────────────────────────────────────────────
  prometheus:
    image: prom/prometheus:v2.48.0
    container_name: weblaunch-prometheus
    restart: unless-stopped
    ports:
      - "${PROMETHEUS_PORT:-9090}:9090"
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=15d"
      - "--web.enable-lifecycle"
      - "--web.enable-admin-api"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./monitoring/prometheus/rules:/etc/prometheus/rules:ro
      - prometheus-data:/prometheus
    networks:
      - monitoring-net
    labels:
      <<: *labels
      com.weblaunch.service: "prometheus"

  # ── Grafana ────────────────────────────────────────────────
  grafana:
    image: grafana/grafana:10.2.2
    container_name: weblaunch-grafana
    restart: unless-stopped
    ports:
      - "${GRAFANA_PORT:-3001}:3000"
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-admin123}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_DOMAIN=localhost
      - GF_ANALYTICS_REPORTING_ENABLED=false
      # ── No login required ─────────────────────────────────────
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_NAME=Main Org.
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
      - GF_AUTH_DISABLE_LOGIN_FORM=true
      - GF_AUTH_BASIC_ENABLED=false
      - GF_SECURITY_ALLOW_EMBEDDING=true
      # ── Home dashboard ─────────────────────────────────────────
      - GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH=/var/lib/grafana/dashboards/weblaunch.json
    volumes:
      - grafana-data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on:
      - prometheus
    networks:
      - monitoring-net
    labels:
      <<: *labels
      com.weblaunch.service: "grafana"

  # ── Node Exporter (host metrics) ───────────────────────────
  node-exporter:
    image: prom/node-exporter:v1.7.0
    container_name: weblaunch-node-exporter
    restart: unless-stopped
    command:
      - "--path.procfs=/host/proc"
      - "--path.sysfs=/host/sys"
      - "--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    ports:
      - "9100:9100"
    networks:
      - monitoring-net
    labels:
      <<: *labels
      com.weblaunch.service: "node-exporter"

  # ── cAdvisor (container metrics) ──────────────────────────
  cadvisor:
    image: gcr.io/cadvisor/cadvisor:v0.47.2
    container_name: weblaunch-cadvisor
    restart: unless-stopped
    privileged: true
    devices:
      - /dev/kmsg
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro
      - /dev/disk/:/dev/disk:ro
    ports:
      - "8080:8080"
    networks:
      - monitoring-net
    labels:
      <<: *labels
      com.weblaunch.service: "cadvisor"
